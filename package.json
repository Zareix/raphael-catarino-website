{
  "name": "dashboard",
  "workspaces": [
    "client",
    "cms"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run develop"
  },
  "devDependencies": {
    "concurrently": "^7.3.0"
  }
}
